---
description: Manually sync Skillfish skills from your workspace
user_invocable: true
---

Run the Skillfish sync script to pull the latest baseline skills from the server.

Use the Bash tool to execute:

```
bash "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/sync.sh"
```

Report the output to the user.
